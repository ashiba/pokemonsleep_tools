# AGENTS.md

このリポジトリは、ゲーム「ポケモンスリープ」のプレイ支援ツール群です。
このファイルには、コードだけからは読み取れない「経緯・意図・運用ルール」を記します。

## プロジェクトの目的と位置づけ

- 個人が自分のために作った、静的な Web ツール集。コンテンツはニッチな部類で、公開先は Cloudflare Pages (`https://pokemonsleep-tools.pages.dev/`)。
- 対象とするのは「料理」まわりが中心。プレイヤーは毎日「カレー(シチュー)+デザート+サラダ」の3品を1食として調理する、というゲーム仕様が前提にある。
- 収益化や大規模ユーザー想定はない。シンプル・素早い動作を最優先する。

## 経緯（時系列）

1. **起点**: 「料理サーチ」1ページ(index.html)として開始。カレー・デザート・サラダから各1品を選び、「食材の種類数上限・最低ratio・NG食材除外」で全組み合わせを検索するものが最初。
2. **初期エナジー表示 + ハイライト食材**: game8(2026-07-31時点)から全レシピの初期エナジーを手作業抽出し、結果カードに表示。ハイライト食材は localStorage(キー: `pokemon-sleep-hl-v1`)で保持。
3. **チップ個数を「和」→「最大値」へ変更**: 当初は3料理の食材個数の合計を表示していたが、ユーザー指示で「max」表示に変更。料理表示も横並び→縦3行へ。
4. **最低エナジーフィルタ追加**: ratioだけでなくエナジーでも絞れるように checkbox 切替で追加。
5. **サイト構造の整理**: ルート(index.html)を「トップページ」化し、料理サーチは `search.html` へ移動。以降ツール群の入口となる。
6. **レシピ食材チェッカー追加** (`recipes.html`): 全レシピを一覧し、「所持食材(入力)で作れるか」をハイライトするページ。料理サーチとは独立した目的。
7. **食材OCR追加**: チェッカーの「食材と個数を入力」に、バッグのスクショから個数を一括入力するOCR（Tesseract.js、位置ベースで19件マッピング、単一画像・全置換・固定閾値175/PSM11）を追加。
8. **Cloudflare Pagesへ移行**: 2026-08に GitHub Pages (`ashiba.github.io`) から Cloudflare Pages (`pokemonsleep-tools.pages.dev`) へ移行。PRプレビューは Cloudflare の自動 Preview Deployment を利用。GitHub Pages と `gh-pages` ブランチは廃止。
9. 現在、この2ページで構成。

## 設計意図・決定事項

- **静的・依存ゼロ・ビルドなし**: バニラ JS, 共通 CSS 1枚。フレームワーク・バンドラは使わない。手間なく載せるため。原則CDN不使用だが、**食材OCRのみ例外**で Tesseract.js を CDN から遅延読込する（初回クリック時のみ、完全ブラウザ内処理で画像は外部送信なし）。
- **データは JSON で同梱**: `food/ingredients.json`(食材名19、正式名・表示順が定義)と `food/recipes/{curry,dessert,salad}.json`。各レシピは `{name, ingredients:[{name,count}], initialEnergy, ratio}`。
- **食材は「正式名」でデータを持つ。表示はページによって略称(ABBR)を使う**。チェッカーでは略称表示(ながねぎ・リンゴ・アボカド等)。検索・判定ロジックは常に正式名基準。
- **色の意味(チェッカー)**: 「作れる = 枠を緑」「足りない食材 = チップをグレー」。「赤枠」は使わない方針(ユーザー指定)。
- **未入力時はハイライトを出さない**(全カード通常表示)。
- **判定仕様**:
  - モード1「食材のみ指定」: レシピの全食材が指定セットに含まれれば「作れる」(個数は無視)。
  - モード2「食材と個数」: 全食材で 必要数 ≤ 所持数 なら「作れる」。
- **並び順**: ratio 降順 → 同率は初期エナジー降順(ユーザー指定を積み重ねた結果)。
- **カテゴリは横3列**(カレー・デザート・サラダ)。モバイルでは1列になり、分析しづらいため「カテゴリ表示ON/OFF」ボタンで絞り込めるようにした。
- **検索の既定値**: 食材6種 / ratio1.25 / エナジー8000(使いながらユーザーが調整した現行値)。

## ゲーム知識(コード内に書いていない前提)

- 「ratio」はレシピ固有の倍率、初期エナジーはそのレシピで得られる基礎エナジー(game8出典)。将来ゲーム更新で変わり得る。
- 料理は必ず3品セット(カレー+デザート+サラダ)で1食になる。料理サーチの「組み合わせ」とはこの3品セットのこと。
- 検索結果はソート日「食材種類数の少なさ」順で表示。…実装はコード参照で良いが、比率の条件厳し過ぎだと0件になる・6種で8件というのはユーザーが「実プレイに合う」と判断した調整結果。

## 運用ルール

- **コミットメッセージ・PRテキストは日本語**。
- **ブランチ運用**: かつて `feature/* → develop → main` だったが、ユーザーにより develop は廃止。現在は **`feature/*` ブランチで実装 → PR → ユーザーが main へマージ**。小さな変更・指示(README等)は main 直コミットで通る場合もある。
- ローカル動作確認はルートで `python3 -m http.server 8001`(cwd=リポジトリ直下)。ポート8000は別プロジェクト(ez_sign_daily)が占有。
- `gh` は無い。PRはブラウザの Compare URL を渡し、ユーザー側でマージしてもらう。認証は SSH(GitHub)。
- コミットはユーザーが明示的に指示したときのみ。push/PR/マージも同じ。
- headless Chrome を使う際は `killall -9 google-chrome`(/`python3`)で終了する。`pkill -f google-chrome` は実行シェル自身にマッチしてハングするため使わない。

## 検証の作法（このプロジェクトで確立した方法）

- `node --check js/*.js` で構文チェック。
- headless Chrome での回帰テストは、実ページのコピー(`/tmp/opencode/site*/`)へ**インラインで抽出用スクリプトを挿入**して行う。`MutationObserver` でデータ読み込み完了を待ち、判定結果を `document.title` に書き出して `--dump-dom` で読む。タイマー頼みは仮想時間で動かないことがあるため使わない。
- サーバーの再起動はアプリ側(`nohup python3 -m http.server 8001`)で行う。

## ファイルマップ（意図レベル）

- `index.html`: トップ。ツールへの入口カードを並べる。
- `search.html` + `js/app.js`: 料理サーチ。
- `recipes.html` + `js/recipes.js` + `js/ocr-ingredient.js`: レシピ食材チェッカー。OCRは `ocr-ingredient.js` に分離し `recipes.html` で遅延読込。
- `css/style.css`: 全ページ共有。`.panel`・`.field`・`.dish`・`.ing-chip`・`.tool-card` 等を共通利用。
- `food/`: 編集リスクの低いデータ層。更新はゲーム更新に合わせて手動。

## 未着手メモ（とりあえず保留）

- レシピデータの抽出スクリプト(game8用)がリポジトリに入っていない。ゲーム更新時の再取得のため、`tools/` に置く案があり。
- 「作れる回数」や「作れるレシピだけ表示」等の拡張案が提案済みだが未実装。